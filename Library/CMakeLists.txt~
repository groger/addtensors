cmake_minimum_required(VERSION 2.6)

file(GLOB LIBRARY_SOURCE *.cc *.h)

include_directories(${PROJECT_SOURCE_DIR}/Library)
include_directories(${CMAKE_CURRENT_SOURCE_DIR})

add_library(library ${LIBRARY_SOURCE})